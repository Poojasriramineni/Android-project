<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RetailerManagementPlatform"
        tools:targetApi="31">
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SalesPersonSignUpActivity">
        </activity>
        <activity android:name=".Chat.PersonalChatActivityManager"
            android:windowSoftInputMode="adjustPan"
            android:label="Personal Chat"></activity>
        <activity android:name=".Chat.PersonalChatActivitySalesperson"
            android:label="Personal Chat"></activity>
        <activity android:name=".Chat.ChatRoom"
            android:windowSoftInputMode="adjustPan"
            android:label="Chat Room"></activity>
        <activity android:name=".TeamLayoutActivity">
        </activity>
        <activity android:name=".ManagerSignupActivity">
        </activity>
    </application>

</manifest>